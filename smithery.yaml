startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    (config) => ({ command: 'docker', args: ['run', '--rm', '-i', 'ghcr.io/wh0ff24/crypto-data-mcp:latest'] })
