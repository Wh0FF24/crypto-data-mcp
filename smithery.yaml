startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    (config) => ({ command: 'uvx', args: ['--from', 'git+https://github.com/Wh0FF24/crypto-data-mcp@main', 'crypto-data-mcp'] })
